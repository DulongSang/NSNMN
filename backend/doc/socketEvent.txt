Chat:

socket emit:
    join,
    message

io emit:
    message,
    userAction,
    systemMsg


NHNMN:

socket emit:
    join,
    mouseDown,  // emit when mouse down
    mousePos,
    color,
    size,
    fill,
    mode,
    fill,
    clear,
    gameMsg

io emit:
    userList,
    round,
    time,
    join,
    mouseDown,
    mousePos,
    color,
    size,
    gameMsg,
    hint